const GRAPH_CONFIG = {
  pageInfo: 'a4',
  gridSize: 10,
  gridEnabled: true,
  gridSteps: 5,
  minGridSize: 1,
  scale: 1.00,
  gridColor: '#acacac',
  gridBackgroundEnabled: true,
  gridBackgroundColor: '#ffffff'
}

export {GRAPH_CONFIG}
